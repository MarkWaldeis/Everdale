# Always push

After finishing a code or asset change in this repo, commit it and `git push` to `origin` on the current branch.

- Do not wait for the user to ask.
- Never force-push.
- Do not commit secrets, leftover screenshots, or debug captures.
- Skip the push only if git is unavailable or the remote rejects the push; then say so.
